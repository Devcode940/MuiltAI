package com.multaihub.app;

import android.app.Application;

/** Application entry point for the Java-only build. */
public final class MultiAIApp extends Application {
    @Override public void onCreate() {
        super.onCreate();
    }
}
